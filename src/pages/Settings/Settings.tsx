import React, { FC } from 'react';
import { Root } from './SettingsStyles';

const Settings: FC = () => {
    return (
        <Root>
            Settings
        </Root>
    );
};

export default Settings;
