import React, { FC } from 'react';
import { Root } from './WelcomeStyles';

const Welcome: FC = () => {
    return (
        <Root>
            Welcome
        </Root>
    );
};

export default Welcome;
